[package]
name = "devvani-reversible"
version = "0.1.0"
edition = "2021"
description = "Reversible Compute Engine for Devvani — tiered ancilla-bit based uncomputation system"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3.0"